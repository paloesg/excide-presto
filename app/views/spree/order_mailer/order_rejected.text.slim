p
  = Spree.t('order_mailer.confirm_email.dear_customer')
p Order ##{@order.number} has been rejected.
p
  ' You can edit your order and submit it again:
  = link_to "Edit #{@order.number}", edit_rejected_url(@order.number)
= render partial: "table_order_txt_version", handlers: [:slim], formats: [:html], locals: { order: @order }
p
  ' Rejected by : #{@order.canceler.email}
  = "(#{@order.canceler.full_name})" if @order.canceler.first_name.present?
br
= render 'footer'