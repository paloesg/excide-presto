h1 Service request

.panel.panel-default
  table.table.table-bordered
    tbody
      tr
        th.col-md-4 Service Name
        td.col-md-8 =@service_request.service_name
      tr
        th Created by
        td = Spree::User.find(@service_request.spree_user_id).email
      - @service_request.fields['text'].each do |label, field|
        tr
          th = label
          td = field
      - @service_request.fields['image'].each do |image|
        tr
          th = image['text']
          td = image_tag main_app.url_for(@service_request.images.find(image['id']).try(:attachment)), class: 'img-responsive'
