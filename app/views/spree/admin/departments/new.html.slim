- content_for :page_title do
  = link_to Spree.t(:departments), spree.admin_departments_url
  '  /
  = Spree.t(:new_department)

= render partial: 'spree/admin/shared/error_messages', locals: { target: @department }

.row
  .col-xs-12.col-md-12
    = form_for ([:admin, @department]), url: spree.admin_departments_url, method: :post do |f|
      = render partial: 'form', locals: { f: f }